% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filtering_functions.R
\name{filter_expression}
\alias{filter_expression}
\title{Filter a DESeq2 object by expression and make plots of the filtering}
\usage{
filter_expression(
  dds,
  outpath,
  group = NULL,
  design = NULL,
  min.count = 10,
  min.total.count = 15,
  large.n = 10,
  min.prop = 0.7,
  normalization = "vst",
  ...
)
}
\arguments{
\item{dds}{DESeq2 object}

\item{outpath}{Path to output directory}

\item{group}{Column of interest for filtering and PCA grouping (optional)}

\item{design}{Design matrix for filtering. Ignored if group is not NULL.}

\item{min.count}{Minimum count required for at least some samples (default: 10)}

\item{min.total.count}{Minimum total count required across all samples (default: 15)}

\item{large.n}{Number of samples per group considered "large" (default: 10)}

\item{min.prop}{Minimum proportion of samples in the smallest group that express the gene (default: 0.7)}

\item{normalization}{Normalization method for saved output (default: "vst")}

\item{...}{Additional arguments to pass to edgeR::filterByExpr}
}
\value{
DESeq2 object after filtering
}
\description{
Filter genes using edgeR's filterByExpr and generate diagnostic plots
}
